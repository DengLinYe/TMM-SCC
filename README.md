# 基于大模型的语义一致约束 TMM

> - 从 `github.com/whdii/TMM.git` 修改而来，原版本至少在我本地无法正常运行，主要做了两部分的前期工作：
>   1. 修复导致无法运行的诸多bug，其中大部分是使用未定义的成员变量、成员函数，即原作者可能没有实现部分函数；
>   2. 同步依赖为更新版本的环境：Python3.12、CUDA13.0、PyTorch2.11、Transformers 4.38.2
